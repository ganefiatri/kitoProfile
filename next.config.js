/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'images.unsplash.com','kito-profile-d5wi.vercel.app']
  }
}

module.exports = nextConfig
